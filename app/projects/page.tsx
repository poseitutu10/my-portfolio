import { Metadata } from "next";
import React from "react";


export const metadata: Metadata = {
  title: "Projects",
  description: "This is Phinehas' showcase project so far"
}

const Projects: React.FC = () => {
  return <div>Projects</div>;
};

export default Projects;
